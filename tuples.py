#i tuples sono simili alle liste
#le differenze con queste ultime sono:
#la lista può fare la stessa cosa
#la lista al contrario del tuple può essere modificata
#di solito usi i tuple per dati non cambiabili o mutati
#creazione di un tuple
coordinates = (4, 5)
#i tuple sono immutabili non possono essere cambiati o modificati
#appena dichiarato non può essere modificat
#stampa dei valori di un tuple
print(coordinates[0])
print(coordinates[1])
#una lista di tuples
tuple_list = [(4, 5), (6, 7), (1, 3)]
